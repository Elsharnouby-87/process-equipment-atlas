# QA Result — 2026-09-13

Status: **PASS**

## Automated verification

- Dependency sanity: PASS
- Ordered source assembly: PASS
- Structural regression QA: PASS
- TypeScript strict check: PASS
- Production Vite build: PASS
- GitHub Pages build: PASS
- GitHub Pages deployment: PASS

## TypeScript defects found and corrected during QA

The stricter QA gate exposed issues that the previous Vite-only build did not surface:

- One unused icon import in `DraftStackPage.tsx`.
- Three unsafe `Box3Helper.material` property accesses caused by the Three.js `Material | Material[]` type.
- One later unsafe highlight opacity access using the same union type.

These were corrected without changing intended runtime behavior.

## Bundle organization

Before cleanup, the main application JavaScript bundle was approximately **1,076 kB minified**.

After vendor chunk organization:

- application chunk: approximately **327 kB minified**
- React vendor chunk: approximately **223 kB minified**
- Three.js vendor chunk: approximately **505 kB minified**
- icon vendor chunk: approximately **16 kB minified**

This improves cache behavior and keeps the application-specific chunk well below its previous size.

## Remaining technical debt

- `Heater3D.tsx` is still a large generated/runtime module.
- `OperationPage.tsx` is still a large training-state module.

Both are intentionally deferred to dedicated refactor checkpoints so code splitting does not get mixed with new simulator behavior.
